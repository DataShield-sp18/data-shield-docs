"use strict";(self.webpackChunkdata_shield_docs=self.webpackChunkdata_shield_docs||[]).push([["3867"],{6089(e,n,t){t.r(n),t.d(n,{metadata:()=>o,default:()=>h,frontMatter:()=>r,contentTitle:()=>a,toc:()=>l,assets:()=>c});var o=JSON.parse('{"id":"engineering/distributed-execution","title":"Distributed execution","description":"How detection and de-identification actually distribute across a Spark","source":"@site/docs/engineering/distributed-execution.md","sourceDirName":"engineering","slug":"/engineering/distributed-execution","permalink":"/data-shield-docs/engineering/distributed-execution","draft":false,"unlisted":false,"editUrl":"https://github.com/DataShield-sp18/data-shield-docs/tree/main/docs/engineering/distributed-execution.md","tags":[],"version":"current","frontMatter":{},"sidebar":"docsSidebar","previous":{"title":"Secure output layer","permalink":"/data-shield-docs/engineering/secure-output-and-vault"},"next":{"title":"Feature: de-identification workflow","permalink":"/data-shield-docs/features/deidentification-workflow"}}'),i=t(4848),s=t(8453);let r={},a="Distributed execution",c={},l=[{value:"The one-sentence mental model",id:"the-one-sentence-mental-model",level:2},{value:"The <code>Executor</code> seam",id:"the-executor-seam",level:2},{value:"Where the executor comes from",id:"where-the-executor-comes-from",level:2},{value:"Detection distribution: chunks, not individual values",id:"detection-distribution-chunks-not-individual-values",level:2},{value:"Why the model itself can&#39;t just be shipped to a worker",id:"why-the-model-itself-cant-just-be-shipped-to-a-worker",level:2},{value:"De-identification distribution: no locking needed, by construction",id:"de-identification-distribution-no-locking-needed-by-construction",level:2},{value:"The core cap: the boundary between &quot;fast&quot; and &quot;took down the host&quot;",id:"the-core-cap-the-boundary-between-fast-and-took-down-the-host",level:2},{value:"Container and network topology",id:"container-and-network-topology",level:2},{value:"What&#39;s proven, what&#39;s not",id:"whats-proven-whats-not",level:2}];function d(e){let n={a:"a",code:"code",em:"em",h1:"h1",h2:"h2",header:"header",li:"li",mermaid:"mermaid",ol:"ol",p:"p",pre:"pre",strong:"strong",...(0,s.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(n.header,{children:(0,i.jsx)(n.h1,{id:"distributed-execution",children:"Distributed execution"})}),"\n",(0,i.jsxs)(n.p,{children:['How detection and de-identification actually distribute across a Spark\ncluster \u2014 the real mechanics, including the specific mistakes made and\nfixed along the way. If you only need "does it scale, and how is it\nconfigured," see ',(0,i.jsx)(n.a,{href:"../features/distributed-execution",children:"the feature-level summary"}),"\ninstead; this page is the engineering internals."]}),"\n",(0,i.jsx)(n.h2,{id:"the-one-sentence-mental-model",children:"The one-sentence mental model"}),"\n",(0,i.jsxs)(n.p,{children:[(0,i.jsx)(n.strong,{children:"Split independent work into chunks, run chunks on many workers at once,\nglue results back in the original order."})," Everything below is elaboration\non that sentence, plus the specific ways this had to stay fail-closed and\nbyte-identical regardless of which executor actually runs it."]}),"\n",(0,i.jsxs)(n.h2,{id:"the-executor-seam",children:["The ",(0,i.jsx)(n.code,{children:"Executor"})," seam"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-python",children:'class Executor(Protocol):\n    def map(self, fn: Callable[[T], R], items: Sequence[T]) -> list[R]:\n        """Apply fn to every element of items; return results in input order."""\n'})}),"\n",(0,i.jsxs)(n.p,{children:["That's the entire contract \u2014 a ",(0,i.jsx)(n.code,{children:"Protocol"}),", not a class hierarchy, because\nnothing else in the codebase needs to know an executor's ",(0,i.jsx)(n.em,{children:"type"}),", only that\nit has ",(0,i.jsx)(n.code,{children:".map()"}),". Two implementations exist: ",(0,i.jsx)(n.code,{children:"SequentialExecutor"}),"\n(",(0,i.jsx)(n.code,{children:"[fn(item) for item in items]"})," \u2014 the pre-distribution code, unchanged, and\nthe default everywhere) and ",(0,i.jsx)(n.code,{children:"SparkExecutor"})," (ships items to Spark, runs\n",(0,i.jsx)(n.code,{children:"fn"})," on however many workers are available, returns results in the same\norder). Distributing a new piece of work later means wrapping it in\n",(0,i.jsx)(n.code,{children:"executor.map(fn, items)"})," \u2014 not inventing a new distribution mechanism."]}),"\n",(0,i.jsx)(n.h2,{id:"where-the-executor-comes-from",children:"Where the executor comes from"}),"\n",(0,i.jsx)(n.mermaid,{value:'flowchart TD\n    A[API route handler] --\x3e|has org context?| B{org found?}\n    B --\x3e|yes| C["get_executor_for_org(org)"]\n    B --\x3e|no| D["get_executor() \u2014 env-var fallback"]\n    C --\x3e E{org.preferred_executor}\n    E --\x3e|sequential| F[SequentialExecutor]\n    E --\x3e|spark| G["SparkExecutor(master, max_cores)"]\n    D --\x3e H{DATA_SHIELD_EXECUTOR env var}\n    H --\x3e|unset / sequential| F\n    H --\x3e|spark| I["SparkExecutor(master, max_cores)"]\n    H --\x3e|anything else| J["raise UnknownExecutorError \u2014 fail closed"]'}),"\n",(0,i.jsxs)(n.p,{children:["Three layers, in priority order: an explicit per-call ",(0,i.jsx)(n.code,{children:"executor="})," argument\n(what the real ",(0,i.jsx)(n.code,{children:"/analyze"})," and ",(0,i.jsx)(n.code,{children:"/deidentify"})," routes use, resolved from the\nrequesting org), the org's own ",(0,i.jsx)(n.code,{children:"preferred_executor"})," setting, and a\nprocess-wide environment-variable fallback for contexts with no org (like\nthe cached, process-wide detection engine's own default). The reason for\nthree layers rather than one: the detection/de-id engines are expensive to\nbuild (a loaded spaCy/Presidio analyzer) and are constructed once per\nprocess and cached \u2014 but the executor choice has to vary ",(0,i.jsx)(n.em,{children:"per request"}),",\nsince different organizations pick different executors. The engine stays a\ncheap shared singleton; the executor, essentially free to construct, is\nthreaded through per call instead."]}),"\n",(0,i.jsxs)(n.p,{children:["An unrecognized executor name \u2014 a typo in an env var, a bad database row \u2014\nraises ",(0,i.jsx)(n.code,{children:"UnknownExecutorError"})," rather than silently falling back to\nsequential. A misconfiguration here is meant to be loud."]}),"\n",(0,i.jsx)(n.h2,{id:"detection-distribution-chunks-not-individual-values",children:"Detection distribution: chunks, not individual values"}),"\n",(0,i.jsxs)(n.p,{children:["Both the Presidio pass and the RoBERTa pass distribute ",(0,i.jsx)(n.strong,{children:"chunks of unique\nvalues"})," \u2014 never one task per individual value. This is the single most\nimportant implementation detail in the whole system, learned the hard way:\nan early version shipped one Spark task per string, and a job that finished\nin 239 seconds sequentially took 666 seconds distributed. The cause had\nnothing to do with the model \u2014 every Spark task pays a fixed serialization/\nscheduling/JVM\u2194Python-boundary cost, and for a task whose real work is a\nfew milliseconds of regex matching, that fixed cost dominates completely\nonce there are thousands of them. Chunking amortizes that fixed cost across\nmany values per task."]}),"\n",(0,i.jsx)(n.mermaid,{value:"sequenceDiagram\n    participant Col as Column analysis\n    participant Exec as Executor\n    participant W as Worker process(es)\n    Col->>Col: unique_values = dedup(column)\n    Col->>Col: chunks = fixed-size split (DS_DETECT_CHUNK_VALUES, default 500)\n    Col->>Exec: executor.map(presidio_fn, chunks)\n    Exec->>W: ship each CHUNK as one task\n    W->>W: analyze every value in the chunk\n    W--\x3e>Exec: results, tagged with original index\n    Exec--\x3e>Col: sorted back into original order"}),"\n",(0,i.jsxs)(n.p,{children:["Chunk count is now ",(0,i.jsx)(n.strong,{children:"fixed-size"})," (values per chunk, via\n",(0,i.jsx)(n.code,{children:"DS_DETECT_CHUNK_VALUES"}),"), not ",(0,i.jsx)(n.code,{children:"cores \xd7 2"}),' \u2014 a deliberate fix after a\n5,000-row file OOM-killed the driver and hung the progress bar at\n"starting\u2026". Fixed-size chunking scales the chunk ',(0,i.jsx)(n.em,{children:"count"})," with the data\n(a 1M-value column becomes ~2,000 chunks, not a handful of 50k-value giants)\nand decouples it from concurrency, which is the actual memory lever."]}),"\n",(0,i.jsxs)(n.p,{children:["Cross-column work is flattened into ",(0,i.jsx)(n.strong,{children:"one"})," work-item list spanning every\ncolumn, so a 10-column file gets real cross-column parallelism instead of\nfinishing column 1 before column 2 even starts \u2014 a Spark task can't itself\nlaunch a nested distributed job, so this has to be one flat ",(0,i.jsx)(n.code,{children:"executor.map()"}),"\ncall, not a loop of many small ones."]}),"\n",(0,i.jsx)(n.h2,{id:"why-the-model-itself-cant-just-be-shipped-to-a-worker",children:"Why the model itself can't just be shipped to a worker"}),"\n",(0,i.jsxs)(n.p,{children:["The loaded spaCy/Presidio analyzer lives on the engine instance; the method\nthat uses it is a ",(0,i.jsx)(n.strong,{children:"bound method"}),", which implicitly captures the entire\nengine object \u2014 including that multi-hundred-MB analyzer \u2014 the moment it's\npassed as a value. Spark ships functions to workers via ",(0,i.jsx)(n.code,{children:"cloudpickle"}),", and a\nlive spaCy ",(0,i.jsx)(n.code,{children:"Language"})," object either fails to pickle outright or is\ncatastrophically slow to. The fix: the function actually shipped to a\nworker is a ",(0,i.jsxs)(n.strong,{children:["plain module-level function with no bound ",(0,i.jsx)(n.code,{children:"self"})]}),". The first\ntime any given worker process calls it, it lazily builds its own fresh\ndetection engine and caches it in a module-level dict keyed by language \u2014\npaid once per worker ",(0,i.jsx)(n.em,{children:"process"}),", reused for every subsequent chunk that\nprocess handles."]}),"\n",(0,i.jsxs)(n.p,{children:["This is exactly why cold-start cost is paid ",(0,i.jsx)(n.strong,{children:"per worker actually used, not\nonce globally"})," \u2014 a job spread across 8 workers pays that model-load cost\n8 times, in parallel. That's precisely why the core cap below matters: more\nworkers means more concurrent cold starts, which is fine until the host\ndoesn't have enough memory to hold that many model copies simultaneously."]}),"\n",(0,i.jsx)(n.h2,{id:"de-identification-distribution-no-locking-needed-by-construction",children:"De-identification distribution: no locking needed, by construction"}),"\n",(0,i.jsxs)(n.p,{children:["De-identification groups operator assignments by field path and ships one\ntask per field group \u2014 each with its ",(0,i.jsx)(n.strong,{children:"own empty"})," token map, no shared\nmutable state across workers."]}),"\n",(0,i.jsx)(n.mermaid,{value:"sequenceDiagram\n    participant P as De-id engine\n    participant Exec as Executor\n    participant W as Worker (per field)\n    participant D as Driver merge\n    P->>Exec: executor.map(worker, per-field work items)\n    Exec->>W: each field group -> its own empty token map\n    W->>W: apply operators to this field only\n    W--\x3e>Exec: value + token map fragment + audit entries\n    Exec--\x3e>D: all fragments, original field order\n    D->>D: merge fragments, raise on any real collision"}),"\n",(0,i.jsxs)(n.p,{children:["This works with ",(0,i.jsx)(n.strong,{children:"zero coordination between workers"}),", because ",(0,i.jsx)(n.code,{children:"tokenize"}),"\nand ",(0,i.jsx)(n.code,{children:"pseudonym"})," are pure functions of ",(0,i.jsx)(n.code,{children:"(salt, value)"})," \u2014 same value, same\nsalt, same output, computed anywhere. Two different fields containing the\nsame original value independently derive the ",(0,i.jsx)(n.em,{children:"same"})," token, not by\nagreement but because the math is identical, so merging fragments is a\nplain union in the common case. ",(0,i.jsx)(n.code,{children:"encrypt"})," needs no merge step at all: it\ndraws a fresh random nonce on every call by design (nonce reuse under the\nsame key would be a real cryptographic weakness) and never touches the\ntoken map \u2014 its ciphertext is self-contained and reversed directly with the\nsession key later."]}),"\n",(0,i.jsxs)(n.p,{children:[(0,i.jsx)(n.strong,{children:"The one real edge case:"})," a hash-slice collision between two different\nvalues, computed independently in two different partitions, is\nastronomically unlikely but not provably impossible. If a naive merge just\noverwrote conflicting entries, one of those two values would silently\nbecome unrecoverable. Instead, the merge step checks every incoming key\nagainst everything accumulated so far and raises immediately if the same\ntoken maps to two different original values \u2014 the whole job fails closed\nrather than shipping a token map that would recover the wrong value for\nsomeone."]}),"\n",(0,i.jsx)(n.h2,{id:"the-core-cap-the-boundary-between-fast-and-took-down-the-host",children:'The core cap: the boundary between "fast" and "took down the host"'}),"\n",(0,i.jsxs)(n.p,{children:[(0,i.jsx)(n.code,{children:"SparkExecutor"})," defaults to ",(0,i.jsx)(n.code,{children:"local[max_cores]"}),", never bare ",(0,i.jsx)(n.code,{children:"local[*]"}),"\n(every host core). This isn't a performance nicety \u2014 during development, an\nuncapped run on a 16-core machine spun up enough concurrent worker\nprocesses, each loading its own copy of spaCy + RoBERTa (roughly 1\u20132 GB\neach), to drive free system RAM down to about 20 MB before the process was\nkilled. An unbounded executor fanning out memory-heavy model loads is a\nself-inflicted denial-of-service, not just an aggressive tuning setting."]}),"\n",(0,i.jsx)(n.p,{children:"The resulting defense is layered, not single-point:"}),"\n",(0,i.jsxs)(n.ol,{children:["\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.code,{children:"max_cores"})," bounds the Spark session's own thread pool ",(0,i.jsx)(n.em,{children:"and"})," the chunk\ncount computed for detection/de-id \u2014 both read it from the executor\nobject, never from the host's raw core count."]}),"\n",(0,i.jsx)(n.li,{children:"A defensive re-clamp at construction time, independent of whatever's\nconfigured: never ask for more parallelism than the host actually has."}),"\n",(0,i.jsxs)(n.li,{children:["Against a ",(0,i.jsx)(n.strong,{children:"real cluster"})," (not local mode), Spark Standalone's default\nscheduling is greedy \u2014 one application claims every core on every\nregistered worker regardless of partition count. ",(0,i.jsx)(n.code,{children:"local[N]"})," only bounds\nthe ",(0,i.jsx)(n.em,{children:"local"})," thread pool; talking to a real cluster master needs\n",(0,i.jsx)(n.code,{children:"spark.cores.max"})," set explicitly, or the per-org cap silently does\nnothing against the cluster's actual resource grant. This is enforced\nin both local and cluster mode today."]}),"\n",(0,i.jsxs)(n.li,{children:["On the cluster-worker side (not the driver), each worker container has\nits own separate cap (",(0,i.jsx)(n.code,{children:"SPARK_WORKER_CORES"}),") \u2014 left unset, a worker\ndefaults to advertising every core its container can see, which is the\nsame oversubscription risk on the other side of the connection."]}),"\n"]}),"\n",(0,i.jsxs)(n.p,{children:["The core cap is deliberately ",(0,i.jsx)(n.strong,{children:"not"})," organization-configurable \u2014 it's a\nsingle platform-wide setting. Letting one org raise its own cap on a shared\ncluster would just mean that org claims a larger slice of every other\norg's compute too."]}),"\n",(0,i.jsx)(n.h2,{id:"container-and-network-topology",children:"Container and network topology"}),"\n",(0,i.jsxs)(n.p,{children:["Today's cluster (",(0,i.jsx)(n.code,{children:"docker-compose.cluster.yml"}),") is one Spark master and\nthree workers on a private, unpublished Docker network \u2014 no port is ever\nexposed to the host, so Spark's driver\u2194executor traffic (which carries the\nsame data the API process holds) never crosses a boundary reachable from\noutside the stack. The API container is a genuine Spark participant, not\njust an HTTP caller into a separate service \u2014 when the org's executor is\n",(0,i.jsx)(n.code,{children:"spark"}),", the API process itself ",(0,i.jsx)(n.em,{children:"is"})," the Spark driver, which is why the\nAPI's own Docker image needs a JDK and ",(0,i.jsx)(n.code,{children:"pyspark"})," at all. Mandatory security\ncontrols (",(0,i.jsx)(n.code,{children:"spark.authenticate"}),", RPC/shuffle encryption) apply whenever a\ncluster URL is actually used, living in the deployment config rather than\nin application code, so they can't accidentally be skipped by a code\nchange that doesn't touch them."]}),"\n",(0,i.jsx)(n.h2,{id:"whats-proven-whats-not",children:"What's proven, what's not"}),"\n",(0,i.jsxs)(n.p,{children:["Concurrent jobs have run and completed successfully against this cluster.\nWhat hasn't been exercised yet: every run so far has kept the driver and\nevery worker on one physical machine, talking over a local Docker bridge \u2014\na real multi-host split, which is exactly what the proposed scale-out\ndirection in ",(0,i.jsx)(n.a,{href:"../architecture/deployment",children:"Deployment"})," would introduce, has\nnot been tested."]})]})}function h(e={}){let{wrapper:n}={...(0,s.R)(),...e.components};return n?(0,i.jsx)(n,{...e,children:(0,i.jsx)(d,{...e})}):d(e)}}}]);